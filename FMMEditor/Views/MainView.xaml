<ma:MetroWindow
    x:Class="FMMEditor.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FMMEditor"
    xmlns:ma="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    Width="1280"
    Height="600"
    Background="{DynamicResource MaterialDesignPaper}"
    BorderBrush="{DynamicResource PrimaryHueMidBrush}"
    BorderThickness="1"
    DataContext="{Binding Source={StaticResource Locator}, Path=Main}"
    FontFamily="{md:MaterialDesignFont}"
    FontSize="12"
    GlowBrush="{DynamicResource PrimaryHueDarkBrush}"
    TextElement.FontWeight="Medium"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TitleBarHeight="20"
    WindowStartupLocation="CenterScreen"
    WindowTitleBrush="{DynamicResource PrimaryHueDarkBrush}"
    mc:Ignorable="d">

    <DockPanel Background="{DynamicResource MaterialDesignPaper}">

        <!--  Top Bar  -->
        <md:ColorZone
            Height="56"
            DockPanel.Dock="Top"
            Mode="PrimaryMid">
            <Grid>
                <TextBlock
                    Margin="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="18"
                    FontWeight="Bold"
                    Foreground="{DynamicResource MaterialDesignLightBackground}"
                    Text="FMM 26 DB Editor" />

                <StackPanel
                    Margin="0,0,16,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <md:PopupBox
                        Margin="4"
                        Padding="0"
                        PlacementMode="BottomAndAlignRightEdges"
                        StaysOpen="False">
                        <md:PopupBox.ToggleContent>
                            <md:PackIcon
                                Width="21"
                                Height="21"
                                Foreground="{DynamicResource MaterialDesignLightBackground}"
                                Kind="Menu" />
                        </md:PopupBox.ToggleContent>

                        <StackPanel>
                            <Button Click="Exit_Click" FontSize="16">
                                <TextBlock Text="Exit" />
                            </Button>
                        </StackPanel>
                    </md:PopupBox>
                </StackPanel>
            </Grid>
        </md:ColorZone>

        <!--  Main Content  -->
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                <TextBlock
                    Margin="0,0,0,16"
                    FontSize="32"
                    FontWeight="Bold"
                    Foreground="{DynamicResource MaterialDesignBody}"
                    Text="FMM 26 DB Editor"
                    TextAlignment="Center" />

                <TextBlock
                    Margin="0,0,0,48"
                    FontSize="16"
                    Foreground="{DynamicResource MaterialDesignBodyLight}"
                    Text="Choose an editor to get started"
                    TextAlignment="Center" />

                <Grid MaxWidth="900">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Competitions Editor Card  -->
                    <md:Card
                        Grid.Column="0"
                        md:ElevationAssist.Elevation="Dp4"
                        Cursor="Hand"
                        MouseLeftButtonUp="OpenCompetitions_Click"
                        UniformCornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border
                                Grid.Row="0"
                                Padding="24,32"
                                Background="{DynamicResource PrimaryHueMidBrush}"
                                CornerRadius="8,8,0,0">
                                <md:PackIcon
                                    Width="64"
                                    Height="64"
                                    HorizontalAlignment="Center"
                                    Foreground="{DynamicResource MaterialDesignLightBackground}"
                                    Kind="Trophy" />
                            </Border>

                            <StackPanel
                                Grid.Row="1"
                                Margin="24"
                                VerticalAlignment="Center">
                                <TextBlock
                                    FontSize="20"
                                    FontWeight="SemiBold"
                                    Text="Competitions"
                                    TextAlignment="Center" />
                                <TextBlock
                                    Margin="0,8,0,16"
                                    FontSize="14"
                                    Foreground="{DynamicResource MaterialDesignBodyLight}"
                                    Text="Edit competitions and league structures"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                                <Button
                                    HorizontalAlignment="Center"
                                    Click="OpenCompetitions_Click"
                                    Content="OPEN"
                                    Style="{StaticResource MaterialDesignFlatMidBgButton}" />
                            </StackPanel>
                        </Grid>
                    </md:Card>

                    <!--  Names Editor Card  -->
                    <md:Card
                        Grid.Column="2"
                        md:ElevationAssist.Elevation="Dp4"
                        Cursor="Hand"
                        MouseLeftButtonUp="OpenNames_Click"
                        UniformCornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border
                                Grid.Row="0"
                                Padding="24,32"
                                Background="{DynamicResource PrimaryHueMidBrush}"
                                CornerRadius="8,8,0,0">
                                <md:PackIcon
                                    Width="64"
                                    Height="64"
                                    HorizontalAlignment="Center"
                                    Foreground="{DynamicResource MaterialDesignLightBackground}"
                                    Kind="TextBox" />
                            </Border>

                            <StackPanel
                                Grid.Row="1"
                                Margin="24"
                                VerticalAlignment="Center">
                                <TextBlock
                                    FontSize="20"
                                    FontWeight="SemiBold"
                                    Text="Names"
                                    TextAlignment="Center" />
                                <TextBlock
                                    Margin="0,8,0,16"
                                    FontSize="14"
                                    Foreground="{DynamicResource MaterialDesignBodyLight}"
                                    Text="Edit player names and name databases"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                                <Button
                                    HorizontalAlignment="Center"
                                    Click="OpenNames_Click"
                                    Content="OPEN"
                                    Style="{StaticResource MaterialDesignFlatMidBgButton}" />
                            </StackPanel>
                        </Grid>
                    </md:Card>

                    <!--  People Editor Card  -->
                    <md:Card
                        Grid.Column="4"
                        md:ElevationAssist.Elevation="Dp4"
                        Cursor="Hand"
                        MouseLeftButtonUp="OpenPeople_Click"
                        UniformCornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border
                                Grid.Row="0"
                                Padding="24,32"
                                Background="{DynamicResource PrimaryHueMidBrush}"
                                CornerRadius="8,8,0,0">
                                <md:PackIcon
                                    Width="64"
                                    Height="64"
                                    HorizontalAlignment="Center"
                                    Foreground="{DynamicResource MaterialDesignLightBackground}"
                                    Kind="AccountGroup" />
                            </Border>

                            <StackPanel
                                Grid.Row="1"
                                Margin="24"
                                VerticalAlignment="Center">
                                <TextBlock
                                    FontSize="20"
                                    FontWeight="SemiBold"
                                    Text="People"
                                    TextAlignment="Center" />
                                <TextBlock
                                    Margin="0,8,0,16"
                                    FontSize="14"
                                    Foreground="{DynamicResource MaterialDesignBodyLight}"
                                    Text="Edit players, staff and their attributes"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                                <Button
                                    HorizontalAlignment="Center"
                                    Click="OpenPeople_Click"
                                    Content="OPEN"
                                    Style="{StaticResource MaterialDesignFlatMidBgButton}" />
                            </StackPanel>
                        </Grid>
                    </md:Card>
                </Grid>

                <TextBlock
                    Margin="0,48,0,0"
                    FontSize="12"
                    Foreground="{DynamicResource MaterialDesignBodyLight}"
                    Text="Version 2.26.0"
                    TextAlignment="Center" />
            </StackPanel>
        </Grid>

    </DockPanel>
</ma:MetroWindow>
