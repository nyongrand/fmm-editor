<ma:MetroWindow
    x:Class="FMMEditor.Views.CompetitionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:FMMEditor.Controls"
    xmlns:converter="clr-namespace:FMMEditor.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FMMEditor"
    xmlns:ma="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mg="https://github.com/fantasticfiasco/mvvm-dialogs"
    Width="1280"
    Height="600"
    mg:DialogServiceViews.IsRegistered="True"
    BorderThickness="1"
    DataContext="{Binding Source={StaticResource Locator}, Path=Competitions}"
    FontFamily="{StaticResource MaterialDesignFont}"
    GlowBrush="{DynamicResource AccentColorBrush}"
    Style="{StaticResource MaterialDesignWindow}"
    TitleBarHeight="20"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Window.LayoutTransform>
        <ScaleTransform CenterX=".5" CenterY=".5" ScaleX=".92" ScaleY=".92" />
    </Window.LayoutTransform>

    <Window.Resources>
        <converter:BooleanConverter x:Key="Boolean" />
        <converter:BytesConverter x:Key="bytes" />
        <converter:GenderConverter x:Key="GenderConverter" />
        <converter:VisibilityConverter x:Key="Visibility" />

        <Style
            x:Key="centerHeaderColumnStyle"
            BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}"
            TargetType="{x:Type TextBlock}">
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style
            x:Key="genderColumnStyle"
            BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}"
            TargetType="{x:Type TextBlock}">
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontFamily" Value="Segoe UI Symbol" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{Binding IsWomen, Converter={StaticResource GenderConverter}, ConverterParameter=Color}" />
        </Style>
    </Window.Resources>

    <Grid>
        <md:DialogHost Identifier="CompetitionsDialogHost" IsOpen="{Binding ShowDialog}">
            <md:DialogHost.DialogContent>

                <md:Card md:ElevationAssist.Elevation="Dp0" UniformCornerRadius="4">
                    <Grid Width="400" Height="280">
                        <StackPanel
                            Margin="16,0"
                            Orientation="Vertical"
                            Visibility="{Binding ShowMoveDialog, Converter={StaticResource Visibility}}">

                            <TextBlock
                                Margin="16,16,16,8"
                                FontSize="16"
                                Text="ADD CLUB"
                                TextAlignment="Center" />

                            <TextBlock
                                FontSize="14"
                                Text="to"
                                TextAlignment="Center" />

                            <TextBlock
                                Margin="0,8,0,0"
                                FontSize="18"
                                Text="{Binding SelectedCompetition.FullName}"
                                TextAlignment="Center" />

                            <controls:FilterableComboBox
                                Margin="0,8,0,0"
                                md:HintAssist.Foreground="{DynamicResource MaterialDesign.Brush.Primary.Light}"
                                md:HintAssist.Hint="Filter club by nation"
                                DisplayMemberPath="Name"
                                FontSize="16"
                                IsEditable="True"
                                IsTextSearchEnabled="False"
                                ItemsSource="{Binding Nations}"
                                OnlyValuesInList="True"
                                SelectedItem="{Binding FilterSwitchNation}"
                                StaysOpenOnEdit="True"
                                Style="{DynamicResource MaterialDesignFilledComboBox}">
                                <controls:FilterableComboBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel VirtualizationMode="Recycling" />
                                    </ItemsPanelTemplate>
                                </controls:FilterableComboBox.ItemsPanel>
                            </controls:FilterableComboBox>

                            <controls:FilterableComboBox
                                Margin="0,8,0,0"
                                md:HintAssist.Foreground="{DynamicResource MaterialDesign.Brush.Primary.Light}"
                                md:HintAssist.Hint="Club to switch"
                                FontSize="16"
                                IsEditable="True"
                                IsTextSearchEnabled="False"
                                ItemsSource="{Binding ClubsAvailableForSwitch}"
                                OnlyValuesInList="True"
                                SelectedItem="{Binding SwitchedWithClub}"
                                StaysOpenOnEdit="True"
                                Style="{DynamicResource MaterialDesignFilledComboBox}">
                                <controls:FilterableComboBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel VirtualizationMode="Recycling" />
                                    </ItemsPanelTemplate>
                                </controls:FilterableComboBox.ItemsPanel>

                                <controls:FilterableComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock
                                                FontSize="10"
                                                Opacity="0.8"
                                                Text="{Binding Competition}" />

                                            <TextBlock
                                                Margin="0,2,0,0"
                                                FontSize="13"
                                                Text="{Binding FullName}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </DataTemplate>
                                </controls:FilterableComboBox.ItemTemplate>
                            </controls:FilterableComboBox>

                            <StackPanel
                                Margin="0,8,0,0"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal">

                                <Button
                                    Width="90"
                                    Height="35"
                                    Margin="0,16,0,0"
                                    HorizontalAlignment="Right"
                                    Command="{Binding CancelSwitch}"
                                    Content="CANCEL"
                                    FontSize="14"
                                    Style="{DynamicResource MaterialDesignFlatButton}" />

                                <Button
                                    Width="90"
                                    Height="35"
                                    Margin="0,16,0,0"
                                    HorizontalAlignment="Right"
                                    Command="{Binding ConfirmMove}"
                                    Content="MOVE"
                                    FontSize="14"
                                    Style="{DynamicResource MaterialDesignFlatButton}" />

                            </StackPanel>
                        </StackPanel>

                        <StackPanel
                            Margin="16,0"
                            Orientation="Vertical"
                            Visibility="{Binding ShowSwitchDialog, Converter={StaticResource Visibility}}">

                            <TextBlock
                                Margin="16,16,16,8"
                                FontSize="16"
                                Text="SWITCH CLUB"
                                TextAlignment="Center" />

                            <TextBlock
                                FontSize="18"
                                Text="{Binding SelectedClub.FullName}"
                                TextAlignment="Center" />

                            <TextBlock
                                Margin="0,8,0,0"
                                FontSize="14"
                                Text="with"
                                TextAlignment="Center" />

                            <controls:FilterableComboBox
                                Margin="0,8,0,0"
                                md:HintAssist.Foreground="{DynamicResource MaterialDesign.Brush.Primary.Light}"
                                md:HintAssist.Hint="Filter club by nation"
                                DisplayMemberPath="Name"
                                FontSize="16"
                                IsEditable="True"
                                IsTextSearchEnabled="False"
                                ItemsSource="{Binding Nations}"
                                OnlyValuesInList="True"
                                SelectedItem="{Binding FilterSwitchNation}"
                                StaysOpenOnEdit="True"
                                Style="{DynamicResource MaterialDesignFilledComboBox}">
                                <controls:FilterableComboBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel VirtualizationMode="Recycling" />
                                    </ItemsPanelTemplate>
                                </controls:FilterableComboBox.ItemsPanel>
                            </controls:FilterableComboBox>

                            <controls:FilterableComboBox
                                Margin="0,8,0,0"
                                md:HintAssist.Foreground="{DynamicResource MaterialDesign.Brush.Primary.Light}"
                                md:HintAssist.Hint="Club to switch"
                                FontSize="16"
                                IsEditable="True"
                                IsTextSearchEnabled="False"
                                ItemsSource="{Binding ClubsAvailableForSwitch}"
                                OnlyValuesInList="True"
                                SelectedItem="{Binding SwitchedWithClub}"
                                StaysOpenOnEdit="True"
                                Style="{DynamicResource MaterialDesignFilledComboBox}">
                                <controls:FilterableComboBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel VirtualizationMode="Recycling" />
                                    </ItemsPanelTemplate>
                                </controls:FilterableComboBox.ItemsPanel>

                                <controls:FilterableComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock
                                                FontSize="10"
                                                Opacity="0.8"
                                                Text="{Binding Competition}" />

                                            <TextBlock
                                                Margin="0,2,0,0"
                                                FontSize="13"
                                                Text="{Binding FullName}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </DataTemplate>
                                </controls:FilterableComboBox.ItemTemplate>
                            </controls:FilterableComboBox>

                            <StackPanel
                                Margin="0,8,0,0"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal">

                                <Button
                                    Width="90"
                                    Height="35"
                                    Margin="0,16,0,0"
                                    HorizontalAlignment="Right"
                                    Command="{Binding CancelSwitch}"
                                    Content="CANCEL"
                                    FontSize="14"
                                    Style="{DynamicResource MaterialDesignFlatButton}" />

                                <Button
                                    Width="90"
                                    Height="35"
                                    Margin="0,16,0,0"
                                    HorizontalAlignment="Right"
                                    Command="{Binding ConfirmSwitch}"
                                    Content="SWITCH"
                                    FontSize="14"
                                    Style="{DynamicResource MaterialDesignFlatButton}" />

                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </md:Card>

            </md:DialogHost.DialogContent>

            <DockPanel
                Margin="0,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="{DynamicResource MaterialDesignPaper}">

                <md:ColorZone
                    Height="56"
                    DockPanel.Dock="Top"
                    Mode="PrimaryMid">

                    <Grid HorizontalAlignment="Stretch">

                        <!--  Search Box  -->
                        <md:ColorZone
                            Margin="10,8,0,8"
                            Padding="10,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Stretch"
                            Panel.ZIndex="1"
                            md:ElevationAssist.Elevation="Dp0"
                            CornerRadius="2"
                            Mode="Standard">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBox
                                    Width="335"
                                    Padding="0,0,8,0"
                                    VerticalAlignment="Stretch"
                                    VerticalContentAlignment="Center"
                                    md:HintAssist.Foreground="{DynamicResource MaterialDesign.Brush.Primary.Light}"
                                    md:HintAssist.Hint="Search Competition Name / Nation"
                                    md:HintAssist.HintOpacity="0.2"
                                    md:TextFieldAssist.DecorationVisibility="Hidden"
                                    BorderThickness="0"
                                    FontSize="16"
                                    IsEnabled="{Binding IsDatabaseLoaded}"
                                    MaxLines="1"
                                    TabIndex="0"
                                    Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" />

                                <Button
                                    Grid.Column="1"
                                    IsEnabled="False"
                                    Style="{StaticResource MaterialDesignToolButton}"
                                    Visibility="{Binding SearchQuery, Converter={StaticResource Visibility}, ConverterParameter=TextInvert}">
                                    <md:PackIcon Kind="Search" />
                                </Button>

                                <Button
                                    Grid.Column="1"
                                    Command="{Binding ClearSearch}"
                                    Style="{StaticResource MaterialDesignToolButton}"
                                    Visibility="{Binding SearchQuery, Converter={StaticResource Visibility}, ConverterParameter=Text}">
                                    <md:PackIcon Kind="Close" Opacity=".56" />
                                </Button>
                            </Grid>
                        </md:ColorZone>

                        <TextBlock
                            Grid.Column="1"
                            Margin="5"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="18"
                            FontWeight="Bold"
                            Text="FMM 26 DB Editor" />

                        <!--  Toolbar Menu  -->
                        <StackPanel
                            Margin="0,0,16,0"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">

                            <md:PopupBox
                                Margin="4"
                                Padding="0"
                                PlacementMode="BottomAndAlignRightEdges"
                                StaysOpen="False">
                                <md:PopupBox.ToggleContent>
                                    <md:PackIcon
                                        Width="21"
                                        Height="21"
                                        Kind="Menu" />
                                </md:PopupBox.ToggleContent>

                                <StackPanel>
                                    <Button Click="Donate_Click" FontSize="16">
                                        <StackPanel Orientation="Horizontal">
                                            <md:PackIcon
                                                Width="18"
                                                Height="18"
                                                Margin="0,0,8,0"
                                                VerticalAlignment="Center"
                                                Kind="CoffeeOutline" />
                                            <TextBlock VerticalAlignment="Center" Text="Support me" />
                                        </StackPanel>
                                    </Button>

                                    <Separator />

                                    <Button
                                        Command="{Binding Load}"
                                        Content="Load Database"
                                        FontSize="16" />

                                    <Button
                                        Command="{Binding Save}"
                                        Content="Save"
                                        FontSize="16"
                                        IsEnabled="{Binding IsDatabaseLoaded}" />

                                    <Button
                                        Command="{Binding SaveAs}"
                                        Content="Save As..."
                                        FontSize="16"
                                        IsEnabled="{Binding IsDatabaseLoaded}" />

                                    <Separator />

                                    <Button
                                        Click="Exit_Click"
                                        Content="Exit"
                                        FontSize="16" />
                                </StackPanel>
                            </md:PopupBox>
                        </StackPanel>

                    </Grid>
                </md:ColorZone>

                <StatusBar
                    Height="24"
                    DockPanel.Dock="Bottom"
                    FontSize="12">
                    <StatusBar.Background>
                        <SolidColorBrush Color="{DynamicResource MahApps.Colors.Flyout}" />
                    </StatusBar.Background>

                    <StatusBarItem>
                        <TextBlock Text="{Binding FolderPath}" />
                    </StatusBarItem>

                    <StatusBarItem>
                        <TextBlock Text="{Binding ClubsInSelectedCompetition.Count}" />
                    </StatusBarItem>
                </StatusBar>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>

                    <!--  Empty State View  -->
                    <Grid
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Background="{DynamicResource MaterialDesignPaper}"
                        Visibility="{Binding IsDatabaseLoaded, Converter={StaticResource Visibility}, ConverterParameter=Inverter}">
                        <StackPanel
                            MaxWidth="600"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">

                            <md:PackIcon
                                Width="80"
                                Height="80"
                                Margin="0,0,0,16"
                                HorizontalAlignment="Center"
                                Kind="DatabaseOff"
                                Opacity="0.5" />

                            <TextBlock
                                Margin="0,0,0,8"
                                FontSize="24"
                                FontWeight="Bold"
                                Text="No Database Loaded"
                                TextAlignment="Center" />

                            <TextBlock
                                Margin="0,0,0,16"
                                FontSize="16"
                                Text="Get started by loading an FM26 database"
                                TextAlignment="Center"
                                TextWrapping="Wrap" />

                            <md:Card Padding="24" md:ElevationAssist.Elevation="Dp2">
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,16"
                                        FontSize="18"
                                        FontWeight="SemiBold"
                                        Text="Instructions"
                                        TextAlignment="Left" />

                                    <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                                        <TextBlock
                                            Width="24"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Text="1."
                                            TextAlignment="Right" />
                                        <TextBlock
                                            Margin="12,0,0,0"
                                            FontSize="14"
                                            Text="Download the FM26 database files"
                                            TextWrapping="Wrap" />
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                                        <TextBlock
                                            Width="24"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Text="2."
                                            TextAlignment="Right" />
                                        <TextBlock
                                            Margin="12,0,0,0"
                                            FontSize="14"
                                            Text="Extract the database files to a folder on your computer"
                                            TextWrapping="Wrap" />
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                                        <TextBlock
                                            Width="24"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Text="3."
                                            TextAlignment="Right" />
                                        <TextBlock
                                            Margin="12,0,0,0"
                                            FontSize="14"
                                            TextWrapping="Wrap">
                                            <Run Text="Click the" />
                                            <Run FontWeight="SemiBold" Text="menu icon (☰)" />
                                            <Run Text="in the top right corner" />
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,0" Orientation="Horizontal">
                                        <TextBlock
                                            Width="24"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Text="4."
                                            TextAlignment="Right" />
                                        <TextBlock
                                            Margin="12,0,0,0"
                                            FontSize="14"
                                            TextWrapping="Wrap">
                                            <Run Text="Select" />
                                            <Run FontWeight="SemiBold" Text="'Load Database'" />
                                            <Run Text="and browse to your extracted folder" />
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </md:Card>

                            <Button
                                Margin="0,32,0,0"
                                HorizontalAlignment="Center"
                                Command="{Binding Load}"
                                Style="{StaticResource MaterialDesignFlatMidBgButton}">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon
                                        Width="20"
                                        Height="20"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center"
                                        Kind="FolderOpen" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Text="LOAD DATABASE" />
                                </StackPanel>
                            </Button>

                        </StackPanel>
                    </Grid>

                    <!--  Data Grids (shown when database is loaded)  -->
                    <DataGrid
                        Grid.Column="0"
                        md:DataGridAssist.SelectedCellBorderBrush="Transparent"
                        AutoGenerateColumns="False"
                        BorderThickness="1"
                        CanUserAddRows="True"
                        CanUserReorderColumns="False"
                        CanUserResizeRows="False"
                        FontSize="12.5"
                        GridLinesVisibility="All"
                        IsSynchronizedWithCurrentItem="True"
                        ItemsSource="{Binding CompetitionsList}"
                        RowHeight="33"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        SelectedItem="{Binding SelectedCompetition}"
                        SelectionMode="Single"
                        Visibility="{Binding IsDatabaseLoaded, Converter={StaticResource Visibility}, ConverterParameter=Boolean}">

                        <!--<DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsWomen}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource WomenCompetitionRowBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsWomen}" Value="False">
                                        <Setter Property="Background" Value="{StaticResource MenCompetitionRowBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>-->

                        <DataGrid.Columns>

                            <DataGridTextColumn
                                Width="65"
                                MinWidth="65"
                                MaxWidth="110"
                                Binding="{Binding Uid}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="UID"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="65"
                                MinWidth="65"
                                MaxWidth="65"
                                Binding="{Binding Level}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Level"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="65"
                                MinWidth="65"
                                MaxWidth="110"
                                Binding="{Binding Reputation}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Rep"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="50"
                                MinWidth="50"
                                MaxWidth="50"
                                Binding="{Binding IsWomen, Converter={StaticResource GenderConverter}, ConverterParameter=Symbol}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource genderColumnStyle}"
                                Header="⚥"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                        <Setter Property="FontSize" Value="18" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="100"
                                MinWidth="100"
                                Binding="{Binding NationName}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Nation"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="400"
                                MinWidth="400"
                                Binding="{Binding FullName}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"
                                Header="Competition Name"
                                IsReadOnly="True" />

                            <!--<DataGridTextColumn
                                Width="*"
                                Binding="{Binding ShortName}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"
                                Header="Competition Name"
                                IsReadOnly="False" />

                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding CodeName}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"
                                Header="Competition Name"
                                IsReadOnly="False" />-->

                        </DataGrid.Columns>
                    </DataGrid>

                    <DataGrid
                        Grid.Column="1"
                        md:DataGridAssist.SelectedCellBorderBrush="Transparent"
                        AutoGenerateColumns="False"
                        BorderThickness="1"
                        CanUserAddRows="False"
                        CanUserReorderColumns="False"
                        CanUserResizeRows="False"
                        ContextMenu="{DynamicResource menu}"
                        FontSize="12.5"
                        GridLinesVisibility="All"
                        HeadersVisibility="All"
                        IsSynchronizedWithCurrentItem="True"
                        ItemsSource="{Binding ClubsInSelectedCompetition}"
                        LoadingRow="DataGrid_LoadingRow"
                        RowHeaderWidth="40"
                        RowHeight="33"
                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                        SelectedItem="{Binding SelectedClub}"
                        SelectionMode="Single"
                        SelectionUnit="FullRow"
                        Visibility="{Binding IsDatabaseLoaded, Converter={StaticResource Visibility}, ConverterParameter=Boolean}">

                        <DataGrid.RowHeaderStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridRowHeader}" TargetType="DataGridRowHeader">
                                <Setter Property="Padding" Value="8,0" />
                            </Style>
                        </DataGrid.RowHeaderStyle>

                        <DataGrid.Resources>
                            <ContextMenu x:Key="menu">

                                <MenuItem Command="{Binding MoveClub}">
                                    <MenuItem.Header>
                                        <TextBlock>
                                            <Run Text="Add a club to" />
                                            <Run FontWeight="SemiBold" Text="{Binding SelectedCompetition.ShortName, Mode=OneWay}" />
                                        </TextBlock>
                                    </MenuItem.Header>
                                </MenuItem>

                                <MenuItem Command="{Binding RemoveClub}">
                                    <MenuItem.Header>
                                        <TextBlock>
                                            <Run Text="Remove" />
                                            <Run FontWeight="SemiBold" Text="{Binding SelectedClub.ShortName, Mode=OneWay}" />
                                            <Run Text="from" />
                                            <Run FontWeight="SemiBold" Text="{Binding SelectedCompetition.ShortName, Mode=OneWay}" />
                                        </TextBlock>
                                    </MenuItem.Header>
                                </MenuItem>

                                <Separator />

                                <MenuItem Command="{Binding SwitchClub}">
                                    <MenuItem.Header>
                                        <TextBlock>
                                            <Run Text="Switch" />
                                            <Run FontWeight="SemiBold" Text="{Binding SelectedClub.ShortName, Mode=OneWay}" />
                                            <Run Text="with..." />
                                        </TextBlock>
                                    </MenuItem.Header>
                                </MenuItem>

                            </ContextMenu>
                        </DataGrid.Resources>

                        <DataGrid.Columns>

                            <DataGridTextColumn
                                Width="60"
                                MinWidth="60"
                                Binding="{Binding Uid}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="UID"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="105"
                                MinWidth="105"
                                Binding="{Binding Nation}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Nation"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!--<DataGridTextColumn
                                Width="105"
                                MinWidth="105"
                                Binding="{Binding Based}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Based"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>-->

                            <DataGridTextColumn
                                Width="70"
                                MinWidth="70"
                                MaxWidth="70"
                                Binding="{Binding Reputation}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Rep"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="60"
                                MinWidth="50"
                                Binding="{Binding Academy}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Aca"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="60"
                                MinWidth="50"
                                Binding="{Binding Facilities}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource centerHeaderColumnStyle}"
                                Header="Fac"
                                IsReadOnly="True">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="280"
                                Binding="{Binding FullName}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"
                                Header="Full Name"
                                IsReadOnly="True" />

                            <DataGridTextColumn
                                Width="110"
                                Binding="{Binding ShortName}"
                                CanUserReorder="False"
                                ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"
                                Header="Short Name"
                                IsReadOnly="True" />

                        </DataGrid.Columns>
                    </DataGrid>

                    <!--  No Clubs Empty State  -->
                    <Grid
                        Grid.Column="1"
                        Background="{DynamicResource MaterialDesignPaper}"
                        Visibility="{Binding HasNoClubs, Converter={StaticResource Visibility}, ConverterParameter=Boolean}">
                        <StackPanel
                            MaxWidth="400"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">

                            <md:PackIcon
                                Width="64"
                                Height="64"
                                Margin="0,0,0,16"
                                HorizontalAlignment="Center"
                                Kind="SoccerField"
                                Opacity="0.3" />

                            <TextBlock
                                Margin="0,0,0,8"
                                FontSize="20"
                                FontWeight="SemiBold"
                                Text="No Clubs in Competition"
                                TextAlignment="Center" />

                            <TextBlock
                                Margin="0,0,0,16"
                                FontSize="14"
                                TextAlignment="Center"
                                TextWrapping="Wrap">
                                <Run Text="The selected competition" />
                                <Run FontWeight="SemiBold" Text="{Binding SelectedCompetition.FullName, Mode=OneWay}" />
                                <Run Text="does not have any clubs assigned to it." />
                            </TextBlock>

                            <!--<Button HorizontalAlignment="Center" Command="{Binding MoveClub}">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon
                                        Width="18"
                                        Height="18"
                                        Margin="0,0,8,0"
                                        VerticalAlignment="Center"
                                        Kind="Plus" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Text="ADD CLUB" />
                                </StackPanel>
                            </Button>-->

                        </StackPanel>
                    </Grid>

                </Grid>

            </DockPanel>

        </md:DialogHost>

        <md:Snackbar
            Grid.ColumnSpan="2"
            Margin="1"
            HorizontalAlignment="Right"
            MessageQueue="{Binding MessageQueue}" />
    </Grid>
</ma:MetroWindow>
