<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FMEditor.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:FMEditor.Views"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Load">
        <ShellContent
            Title="One"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="OnePage" />
    </FlyoutItem>

    <FlyoutItem Title="Nation" IsEnabled="{Binding EnableNation}">
        <ShellContent
            Title="Nation"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="NationPage" />
    </FlyoutItem>

    <FlyoutItem Title="Competition" IsEnabled="{Binding EnableCompetition}">
        <ShellContent
            Title="Competition"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="CompetitionPage" />
    </FlyoutItem>

    <FlyoutItem Title="Club" IsEnabled="{Binding EnableClub}">
        <ShellContent
            Title="Club"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="ClubPage" />
    </FlyoutItem>

</Shell>
